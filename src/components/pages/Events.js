import React from 'react';

// TODO: Figure out passing params in onClick
 const Events = () => {
    return (
        <div>
            <h1>Events Page!</h1>
        </div>
    );
};

export default Events;