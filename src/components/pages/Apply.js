import React from 'react';

// TODO: Figure out passing params in onClick
 const Apply = () => {
    return (
        <div>
            <h1>Apply Page!</h1>
        </div>
    );
};

export default Apply;