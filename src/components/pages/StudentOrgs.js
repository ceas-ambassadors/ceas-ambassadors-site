import React from 'react';

// TODO: Figure out passing params in onClick
 const StudentOrgs = () => {
    return (
        <div>
            <h1>Student Orgs Page!</h1>
        </div>
    );
};

export default StudentOrgs;